/// Configuración global de la aplicación admin
class AppConfig {
  // Frecuencia unificada de actualización para todas las pantallas (en segundos)
  static const int refreshIntervalSeconds = 30;
  
  // Frecuencia especial para mapa en tiempo real (más frecuente)
  static const int realtimeMapRefreshIntervalSeconds = 5;
  
  // Frecuencia de actualización del dashboard
  static const int dashboardRefreshIntervalSeconds = 30;
}

